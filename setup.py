from setuptools import setup, find_packages

setup(
    name="kolors",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # 这里可以添加你的依赖包
    ],
    python_requires=">=3.6",
) 